# This will be called from a separate terminal profile, so that tmux is
# automatically attached to, using the bash option --rcfile.

# But first source the regular files
. /etc/bash.bashrc
. ~/.bashrc

# Now attach to tmux.
# Using 'exec' replaces the shell with the given command. This is done so that
# detaching from tmux will exit the terminal session automatically.
if [[ -z $TMUX ]] && [[ $- = *i* ]]; then
    #tmux attach-session -t general || tmux attach || tmux; exit;
    exec tmux -2 attach-session -t general
fi
