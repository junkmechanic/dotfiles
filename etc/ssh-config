Host [host nickname]
    Hostname [address]
    User [username on the host]
    IdentityFile ~/.ssh/[RSA/DSA private key]

-----Optional-----

Host [host nickname]
    ForwardAgent yes [to forward ssh agent]
    ServerAliveInterval 180 [to preserve the connection]
