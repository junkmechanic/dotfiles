## Openbox autostart.sh
## ====================

## Set root window colour
hsetroot -solid "#424F5A" &

nitrogen --restore &

compton &

(sleep 4s && tint2) &

(sleep 5s && volumeicon) &

(sleep 5s && clipit) &

(sleep 5s && mate-power-manager) &

xfce4-volumed &

thunar --daemon &

synclient VertEdgeScroll=1 &
synclient TapButton1=1 &

xscreensaver -no-splash &

## Set keyboard settings - 250 ms delay and 25 cps (characters per second) repeat rate.
## Adjust the values according to your preferances.
xset r rate 250 25 &

## Turn on/off system beep
xset b off &

(sleep 10s && conky -q -c ~/.conky/conkyrc-stats) &

xmodmap /home/ankur/.Xmodmap &
setxkbmap -option 'caps:ctrl_modifier' &
xcape -e 'Caps_Lock=Escape' &

# To disable touchpad for 1 sec while typing
syndaemon -i 1 -t -d &

## Script for showdesktop
# ~/bin/showdesktop.sh &
