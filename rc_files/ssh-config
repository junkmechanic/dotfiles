Host [host nickname]
    Hostname [address]
    User [username on the host]
    IdentityFile ~/.ssh/[RSA/DSA private key]

Host [host nickname]
    ServerAliveInterval 180[or whatever you think is sufficient enough interval
for it to ping at to maintain the connection]
